import { expect } from 'chai';

describe('Multiply', () => {
  it('should multiply numbers', () => {
    expect(2 * 2).to.eql(4);
  });
  it('should pass', () => {
    expect(20).to.equal(20);
  });
});
